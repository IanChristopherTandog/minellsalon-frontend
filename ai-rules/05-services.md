# Services Rules

## DO
- Filter by `isActive` for public pages
- Show all (including inactive) in admin
- Include duration and price in displays
- Use category-based filtering

## DON'T
- Don't delete services (deactivate instead)
- Don't show inactive services to clients
- Don't forget `pointsEarned` field for loyalty
