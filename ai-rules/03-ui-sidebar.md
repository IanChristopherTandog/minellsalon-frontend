# Sidebar UI Rules

## DO
- Use `ScrollArea` for scrollable navigation
- Keep profile info in fixed footer
- Use `Collapsible` for nested groups
- Highlight active route with `bg-sidebar-accent`

## DON'T
- Don't let profile overlap navigation
- Don't hide toggle button completely
- Don't forget mobile overlay backdrop
